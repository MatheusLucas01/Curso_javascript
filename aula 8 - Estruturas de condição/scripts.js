// Estruturas de condição, operador condicional ternário

let idade = 18

// Se idade for maior ou igual a 18 então você pode dirigir, se não você não pode
console.log(
    idade >= 18 // Condição
        ? "Você pode dirigir" // Se verdadeiro
        : "Você não pode") // Se falso

// Se nome === (Conteúdo e tipo), for igual a Lucas, então olá Matheus, se não -> Voce não é Matheus
let nome = "Lucas"
console.log(
    nome === "Matheus"
        ? "olá Matheus."
        : "voce não é Matheus")

let object = "Lapis"
console.log(
    object === "Caneta"
    ? "Essa é uma caneta"
    : "Essa não é uma caneta!"
)